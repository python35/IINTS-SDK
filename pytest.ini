[pytest]
testpaths = tests
norecursedirs = .git .hypothesis .mypy_cache .pytest_cache .tox .venv venv build dist examples node_modules
markers =
    performance: latency and throughput regression checks
