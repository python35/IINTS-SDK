# v0.1.21

Release highlights:

- Added `iints sources` CLI command to expose the SDK evidence manifest.
- Added packaged source manifest: `src/iints/presets/evidence_sources.yaml`.
- Added evidence mapping doc: `docs/EVIDENCE_BASE.md`.
- Linked evidence docs across README/manual/technical docs for audit-ready traceability.
- Added regression tests for the new `sources` command.
- Added report-level method note in generated PDFs pointing to evidence docs and CLI source export.

See `CHANGELOG.md` for the full technical list.
