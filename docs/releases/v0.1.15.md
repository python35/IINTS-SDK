# Release v0.1.15

## Highlights
- Hardened dataset registry loading for Python 3.14+.
- Preset CLI now defaults outputs to the working directory and fixes file links.
- Notebooks updated for Python 3.9 compatibility and clearer data‑registry flow.

## Notes
- No breaking API changes.
- Tested with `pytest` and `mypy`.
