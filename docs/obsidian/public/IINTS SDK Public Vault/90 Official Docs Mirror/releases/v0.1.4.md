# Release v0.1.4

## Highlights
- CI-stable PDF regression test (headless Matplotlib `Agg` backend).
- Release tooling in `Makefile` (build/publish/tag/release targets).
- Version bump to `0.1.4`.

## Notes
- PDF generation still emits `fpdf2` deprecation warnings; these are non-fatal.
