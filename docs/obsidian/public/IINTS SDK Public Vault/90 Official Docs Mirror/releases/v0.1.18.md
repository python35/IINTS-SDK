# v0.1.18

Release highlights:

- Research AI track updates (data prep, training, baselines, uncertainty).
- Safety improvements (fail‑soft sensor handling, safer overrides, clamps).
- Demo showcase + ONNX export tooling.

See CHANGELOG.md for the full list.
